<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <com.example.a50beees.ui.SandboxView
        android:id="@+id/sandbox_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"

        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:background="@drawable/hedgehod_god"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/sandbox_recycler_view"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        android:orientation="horizontal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="10dp"
        android:layout_marginVertical="10dp"
        android:padding="10dp"
        android:background="@drawable/recycler_border_with_background"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <FrameLayout
        android:id="@+id/frame"
        android:orientation="vertical"
        android:layout_width="50dp"
        android:layout_height="300dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <com.google.android.material.slider.Slider
            android:id="@+id/time_slider"
            android:layout_width="300dp"
            android:layout_height="50dp"
            android:rotation="270"
            android:layout_gravity="center"

            android:valueFrom="0.01"
            android:valueTo="2.0"
            android:value="1.0"
            android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar"
            app:trackColor="@color/black"
            app:haloColor="@color/primary"
            app:thumbColor="@color/primary"/>

    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>